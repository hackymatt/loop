"use client";

import NewsletterUnsubscribe from "../newsletter/newsletter-unsubscribe";

// ----------------------------------------------------------------------

export default function NewsletterUnsubscribeView({ id }: { id: string }) {
  return <NewsletterUnsubscribe id={id} />;
}
